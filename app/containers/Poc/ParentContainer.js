import React, { PropTypes } from 'react'
import { Parent } from 'components'

const ParentContainer = React.createClass({
  render () {
    return (
      <Parent />
    )
  }
})

export default ParentContainer
